# 选题雷达私有配置模板

> 这是不含真实值的模板。安装后，把它复制为当前 Profile 下的 `local/topic-radar.local.md`，再填写你自己的资料位置。`local/` 已被 `.gitignore` 排除，不要把填写后的文件上传到 GitHub。

## 配置状态

- 状态：`NOT_CONFIGURED`
- 时区：`<例如 Asia/Shanghai>`
- 账号或项目名称：`<仅写本地识别名称>`

填写完成并逐项验证后，把状态改为 `READY`。状态不是 `READY` 时，Agent 不应启动正式选题。

## 账号定位与输入

- 账号定位文件：`<绝对路径>`
- 目标读者文件：`<绝对路径；如果已包含在定位文件中可写“同上”>`
- 搜索关键词文件：`<绝对路径>`
- 历史素材目录：`<绝对路径>`
- 历史选题文件：`<绝对路径>`
- 草稿目录：`<绝对路径；可选>`
- 已发布内容或复盘目录：`<绝对路径；可选>`

## 输出

- 素材卡目录：`<绝对路径>`
- 选题池文件：`<绝对路径>`
- 每日选题报告：`<绝对路径>`
- 写入规则：`只追加，不覆盖；写入后重新读取核对`

## 外部服务与隐私

- 允许的公开搜索方式：`<Hermes Tool Gateway、Tavily 或其他已配置工具>`
- 允许外发的内容：`非敏感关键词、搜索查询、公开 URL`
- 禁止外发的内容：`原始笔记、草稿、评论、个人信息、私有路径、密钥`
- 单次运行搜索上限：`<按预算填写>`
- 失败重试上限：`<按预算填写，建议保持较小>`

## 人工确认

- 最终选题确认人：`<本人或角色名称>`
- 遇到以下情况必须暂停：来源冲突、证据不足、疑似重复、需要非公开信息、写入失败。

## 部署者自检

- [ ] 所有必填路径存在且可读；输出位置可写。
- [ ] 模型和搜索工具已在 Hermes 私有配置中可用。
- [ ] 真实密钥没有写进本文件、SOUL.md 或公开仓库。
- [ ] 已先用公开样例完成一次端到端测试。
